// ============================================================
// GitAtlas — Branching Commands
// git branch, git switch, git checkout, git worktree, git sparse-checkout
// ============================================================

import type { GitCommand } from '../../types';

export const branchingCommands: GitCommand[] = [
  {
    id: 'branch',
    name: 'branch',
    executable: 'git',
    category: 'branching',
    subcategory: 'Branch Management',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'safe',
    summary: 'List, create, rename, or delete branches',
    description: 'git branch is the primary command for managing branches. Without arguments, it lists local branches. With a name, it creates a new branch. With -d or -D, it deletes a branch. Branches are lightweight pointers to commits — creating one is nearly instantaneous.',
    whyItExists: 'Branching is core to Git\'s workflow. Developers create branches to isolate feature work, bug fixes, and experiments without affecting the main codebase. git branch manages these pointers.',
    whenToUse: [
      'Creating a new branch for a feature or bug fix',
      'Listing all local or remote branches',
      'Deleting branches after they\'ve been merged',
      'Renaming branches',
    ],
    whenNotToUse: [
      'When you want to create AND switch to a new branch — use git switch -c or git checkout -b',
    ],
    syntax: [
      'git branch',
      'git branch <name>',
      'git branch -d <name>',
      'git branch -D <name>',
      'git branch -m <old> <new>',
      'git branch -a',
      'git branch -r',
      'git branch -v',
    ],
    options: [
      { flag: '-a', shortFlag: '--all', description: 'List both local and remote-tracking branches.', example: 'git branch -a' },
      { flag: '-r', shortFlag: '--remotes', description: 'List remote-tracking branches only.', example: 'git branch -r' },
      { flag: '-d', shortFlag: '--delete', description: 'Delete a branch that has been fully merged.', example: 'git branch -d feature-auth', warning: 'Refuses to delete if the branch has unmerged commits' },
      { flag: '-D', description: 'Force-delete a branch regardless of merge status.', example: 'git branch -D experimental', warning: 'Permanently removes the branch pointer — commits may become unreachable' },
      { flag: '-m', shortFlag: '--move', description: 'Rename a branch.', example: 'git branch -m old-name new-name' },
      { flag: '-v', shortFlag: '--verbose', description: 'Show the last commit on each branch.', example: 'git branch -v' },
      { flag: '--merged', description: 'List branches that have been merged into the current branch.', example: 'git branch --merged', whenUseful: 'Finding branches safe to delete' },
      { flag: '--no-merged', description: 'List branches that have NOT been merged.', example: 'git branch --no-merged' },
      { flag: '-u <upstream>', shortFlag: '--set-upstream-to', description: 'Set the upstream tracking branch.', example: 'git branch -u origin/main', whenUseful: 'After creating a local branch that should track a remote' },
    ],
    examples: [
      { title: 'List local branches', command: 'git branch', output: '  feature-auth\n* main\n  hotfix-login' },
      { title: 'Create a new branch', command: 'git branch feature-payments' },
      { title: 'Delete a merged branch', command: 'git branch -d feature-auth' },
      { title: 'List merged branches', command: 'git branch --merged', output: '  feature-done\n* main' },
      { title: 'Rename current branch', command: 'git branch -m main trunk' },
    ],
    situations: [
      { description: 'Cleaning up old branches', command: 'git branch --merged | grep -v main | xargs git branch -d', explanation: 'Lists branches merged into the current branch, excludes main, and deletes them.' },
      { description: 'Setting upstream for a new branch', command: 'git push -u origin feature-x', explanation: 'Pushes the branch and sets origin/feature-x as its upstream tracking branch.' },
    ],
    mistakes: [
      'Using -D (force delete) when -d would refuse — you might lose unmerged work.',
      'Deleting a branch that still has unmerged work without realizing it.',
      'Forgetting that git branch <name> only creates the branch — it doesn\'t switch to it.',
    ],
    relatedCommands: ['switch', 'checkout', 'merge', 'rebase'],
    comparisonCommands: ['switch', 'checkout'],
    tips: [
      'git branch <name> creates but doesn\'t switch. Use git switch -c <name> to create and switch.',
      'Use git branch --merged to find branches safe to delete.',
      'Branch names are case-sensitive.',
    ],
    officialReference: 'https://git-scm.com/docs/git-branch',
  },

  {
    id: 'switch',
    name: 'switch',
    executable: 'git',
    category: 'branching',
    subcategory: 'Navigation',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'safe',
    summary: 'Switch to a different branch',
    description: 'git switch changes the current branch. It was introduced in Git 2.23 as a clearer alternative to git checkout for branch switching. With -c, it creates a new branch and switches to it.',
    whyItExists: 'git checkout was overloaded — it both switched branches and restored files. git switch was created specifically for branch switching, making the intent unambiguous.',
    whenToUse: [
      'Switching to an existing branch',
      'Creating a new branch and switching to it',
      'Switching to a remote branch for the first time',
    ],
    whenNotToUse: [
      'When you want to restore file contents — use git restore',
      'When you need to detach HEAD to a specific commit — use git checkout <commit>',
    ],
    syntax: [
      'git switch <branch>',
      'git switch -c <new-branch>',
      'git switch -c <new-branch> <start-point>',
      'git switch -',
    ],
    options: [
      { flag: '-c', shortFlag: '--create', description: 'Create a new branch and switch to it.', example: 'git switch -c feature-auth', whenUseful: 'Starting work on a new feature' },
      { flag: '-C', shortFlag: '--force-create', description: 'Create or reset a branch and switch to it.', example: 'git switch -C feature-auth origin/main', whenUseful: 'Resetting a branch to start from a different point' },
      { flag: '-', description: 'Switch to the previously checked out branch.', example: 'git switch -', whenUseful: 'Quickly toggling between two branches' },
      { flag: '--detach', description: 'Switch to a commit in detached HEAD state.', example: 'git switch --detach abc1234' },
      { flag: '--discard-changes', description: 'Discard local changes when switching.', example: 'git switch --discard-changes main', warning: 'Permanently discards uncommitted changes' },
    ],
    examples: [
      { title: 'Switch to an existing branch', command: 'git switch main' },
      { title: 'Create and switch to a new branch', command: 'git switch -c feature-payments' },
      { title: 'Switch to previous branch', command: 'git switch -' },
      { title: 'Create branch from a specific commit', command: 'git switch -c hotfix abc1234' },
    ],
    situations: [
      { description: 'Starting a new feature', command: 'git switch -c feature-new-dashboard', explanation: 'Creates a new branch from the current position and switches to it.' },
      { description: 'Quickly checking something on main', command: 'git switch main\n# check something\ngit switch -', explanation: 'Switch to main, do your check, then switch back with git switch -.' },
    ],
    mistakes: [
      'Trying to switch with uncommitted changes that conflict — either commit, stash, or use --discard-changes.',
      'Using git switch on Git versions older than 2.23 — it doesn\'t exist. Use git checkout.',
    ],
    relatedCommands: ['branch', 'checkout', 'stash'],
    comparisonCommands: ['checkout'],
    tips: [
      'git switch - is like cd - in the terminal — it goes back to the previous branch.',
      'Requires Git 2.23+. For older versions, use git checkout -b for create-and-switch.',
    ],
    officialReference: 'https://git-scm.com/docs/git-switch',
  },

  {
    id: 'checkout',
    name: 'checkout',
    executable: 'git',
    category: 'branching',
    subcategory: 'Navigation',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'caution',
    summary: 'Switch branches or restore working tree files (legacy multi-purpose command)',
    description: 'git checkout is the traditional command for switching branches AND restoring files. Since Git 2.23, its functionality has been split into git switch (branch switching) and git restore (file restoration). git checkout still works and is widely used, but the newer commands are more explicit about intent.',
    whyItExists: 'git checkout was the original command for both navigating branches and restoring file contents. It remains for backward compatibility and is still the most commonly used in tutorials and documentation.',
    whenToUse: [
      'On Git versions older than 2.23',
      'When you\'re accustomed to the checkout workflow',
      'Creating a new branch and switching to it (-b)',
      'Checking out a specific commit in detached HEAD',
    ],
    whenNotToUse: [
      'On Git 2.23+ when you want clearer intent — use git switch or git restore',
    ],
    syntax: [
      'git checkout <branch>',
      'git checkout -b <new-branch>',
      'git checkout -- <file>',
      'git checkout <commit>',
      'git checkout -b <new-branch> <start-point>',
    ],
    options: [
      { flag: '-b <branch>', description: 'Create a new branch and switch to it.', example: 'git checkout -b feature-login', whenUseful: 'Starting a new feature branch' },
      { flag: '-B <branch>', description: 'Create or reset a branch and switch to it.', example: 'git checkout -B feature-login origin/main' },
      { flag: '-- <file>', description: 'Restore a file to its last committed state (discard changes).', example: 'git checkout -- src/app.ts', warning: 'Permanently discards working directory changes to the file' },
      { flag: '--orphan <branch>', description: 'Create a new branch with no history.', example: 'git checkout --orphan gh-pages', whenUseful: 'Creating a GitHub Pages branch or a clean starting point' },
      { flag: '-t', shortFlag: '--track', description: 'Set up tracking when checking out a remote branch.', example: 'git checkout -t origin/feature' },
    ],
    examples: [
      { title: 'Switch to a branch', command: 'git checkout main' },
      { title: 'Create and switch to a new branch', command: 'git checkout -b feature-auth' },
      { title: 'Restore a file', command: 'git checkout -- src/broken.ts' },
      { title: 'Detach HEAD at a specific commit', command: 'git checkout abc1234' },
    ],
    situations: [
      { description: 'You want to inspect an old commit', command: 'git checkout abc1234', explanation: 'Puts you in detached HEAD state at that commit. You can look around but should create a branch to keep changes.' },
    ],
    mistakes: [
      'Confusing git checkout <branch> with git checkout -- <file> — one switches branches, the other discards file changes.',
      'Forgetting the -- before filenames — can accidentally switch to a branch if a branch has the same name as a file.',
      'Being in detached HEAD and making commits without creating a branch — those commits become hard to find.',
    ],
    warnings: [
      'git checkout -- <file> permanently discards working directory changes. Consider git restore for clearer intent.',
    ],
    relatedCommands: ['switch', 'restore', 'branch'],
    comparisonCommands: ['switch', 'restore'],
    tips: [
      'Since Git 2.23, prefer git switch for branches and git restore for files.',
      'The -- separator clarifies intent: git checkout -- file.txt always means "restore this file".',
    ],
    officialReference: 'https://git-scm.com/docs/git-checkout',
  },

  {
    id: 'worktree',
    name: 'worktree',
    executable: 'git',
    category: 'branching',
    subcategory: 'Advanced Branching',
    type: 'porcelain',
    difficulty: 'advanced',
    dangerLevel: 'safe',
    summary: 'Manage multiple working trees attached to the same repository',
    description: 'git worktree lets you check out multiple branches simultaneously in separate directories, all sharing the same .git repository. This avoids the need to stash or commit before switching branches and lets you work on multiple features in parallel.',
    whyItExists: 'Developers often need to work on multiple things at once — reviewing a PR, hotfixing production, and developing a feature. Without worktrees, you\'d need to stash, commit WIP, or clone the repo again. Worktrees let you have separate directories for each branch.',
    whenToUse: [
      'Working on multiple branches simultaneously',
      'Reviewing or testing a branch without disrupting your current work',
      'Running a long build on one branch while coding on another',
      'Hotfixing production while in the middle of feature work',
    ],
    whenNotToUse: [
      'For simple branch switches — git switch is sufficient',
      'When you need completely isolated Git configurations — use separate clones',
    ],
    syntax: [
      'git worktree add <path> [<branch>]',
      'git worktree list',
      'git worktree remove <worktree>',
      'git worktree prune',
    ],
    options: [
      { flag: 'add <path> <branch>', description: 'Create a new worktree checking out the specified branch.', example: 'git worktree add ../hotfix hotfix-branch', whenUseful: 'Creating a separate directory for a hotfix while staying on your feature branch' },
      { flag: 'add -b <new-branch> <path>', description: 'Create a new branch and a new worktree for it.', example: 'git worktree add -b feature-x ../feature-x' },
      { flag: 'list', description: 'List all worktrees.', example: 'git worktree list' },
      { flag: 'remove <worktree>', description: 'Remove a worktree and its directory.', example: 'git worktree remove ../hotfix' },
      { flag: 'prune', description: 'Clean up stale worktree metadata.', example: 'git worktree prune' },
    ],
    examples: [
      { title: 'Create a worktree for a hotfix', command: 'git worktree add ../hotfix main\ncd ../hotfix\n# Make your fix on main without leaving your feature branch' },
      { title: 'List all worktrees', command: 'git worktree list', output: '/home/dev/project     abc1234 [main]\n/home/dev/hotfix      def5678 [hotfix]' },
      { title: 'Remove a worktree', command: 'git worktree remove ../hotfix' },
    ],
    situations: [
      { description: 'You need to fix a production bug while in the middle of feature work', command: 'git worktree add ../hotfix main\ncd ../hotfix\n# fix, commit, push\ncd ../project\ngit worktree remove ../hotfix', explanation: 'Creates a separate directory with main checked out. Fix and push the hotfix, then remove the worktree and continue feature work.' },
    ],
    mistakes: [
      'Trying to check out the same branch in two worktrees — Git prevents this.',
      'Forgetting to clean up worktrees — use git worktree list and git worktree remove.',
    ],
    relatedCommands: ['branch', 'switch', 'checkout', 'stash'],
    tips: [
      'Worktrees share the same .git — commits, stashes, and refs are shared across all worktrees.',
      'You cannot have the same branch checked out in two worktrees simultaneously.',
    ],
    officialReference: 'https://git-scm.com/docs/git-worktree',
  },

  {
    id: 'sparse-checkout',
    name: 'sparse-checkout',
    executable: 'git',
    category: 'branching',
    subcategory: 'Advanced',
    type: 'porcelain',
    difficulty: 'advanced',
    dangerLevel: 'safe',
    summary: 'Check out only a subset of files from the repository',
    description: 'git sparse-checkout lets you check out only specific directories or files from a large repository. This is useful for monorepos where you only need a subset of the codebase. The rest of the repository\'s files exist in Git\'s history but aren\'t materialized in your working directory.',
    whyItExists: 'In large monorepos (like those at Google, Microsoft, or Meta), the full working directory can be enormous. sparse-checkout lets developers work with only the portion they need, dramatically reducing disk usage and IDE indexing time.',
    whenToUse: [
      'Working in a large monorepo and only needing specific directories',
      'Reducing disk usage for huge repositories',
      'Speeding up IDE indexing by limiting visible files',
    ],
    whenNotToUse: [
      'Small repositories where the full checkout is manageable',
      'When you need frequent access to many different parts of the repo',
    ],
    syntax: [
      'git sparse-checkout init',
      'git sparse-checkout set <dir>...',
      'git sparse-checkout add <dir>...',
      'git sparse-checkout list',
      'git sparse-checkout disable',
    ],
    options: [
      { flag: 'init', description: 'Initialize sparse-checkout mode.', example: 'git sparse-checkout init --cone' },
      { flag: 'set <dirs>', description: 'Set which directories to include.', example: 'git sparse-checkout set src/frontend docs/', whenUseful: 'Specifying exactly which parts of the monorepo you need' },
      { flag: 'add <dirs>', description: 'Add additional directories without removing existing ones.', example: 'git sparse-checkout add src/shared' },
      { flag: 'list', description: 'Show which directories are included.', example: 'git sparse-checkout list' },
      { flag: 'disable', description: 'Disable sparse-checkout and restore the full working directory.', example: 'git sparse-checkout disable' },
      { flag: '--cone', description: 'Use cone mode (recommended). Matches entire directory trees rather than individual file patterns.', example: 'git sparse-checkout init --cone' },
    ],
    examples: [
      { title: 'Set up sparse checkout', command: 'git clone --filter=blob:none --sparse https://github.com/org/monorepo.git\ncd monorepo\ngit sparse-checkout set packages/my-app shared/utils' },
      { title: 'Add more directories', command: 'git sparse-checkout add packages/common' },
      { title: 'See current sparse config', command: 'git sparse-checkout list' },
    ],
    situations: [
      { description: 'Working on one package in a monorepo', command: 'git clone --filter=blob:none --sparse https://github.com/org/monorepo.git\ncd monorepo\ngit sparse-checkout init --cone\ngit sparse-checkout set packages/frontend', explanation: 'Clones the repo without downloading all blobs, then only checks out the frontend package.' },
    ],
    mistakes: [
      'Forgetting to use --cone mode — the older sparse-checkout patterns are error-prone.',
      'Not combining with partial clone (--filter=blob:none) — without it, Git still downloads all objects.',
    ],
    relatedCommands: ['clone', 'worktree'],
    tips: [
      'Combine with partial clone for maximum effect: git clone --filter=blob:none --sparse <url>',
      'Requires Git 2.25+ for the improved sparse-checkout subcommand. Git 2.26+ recommended for cone mode.',
    ],
    officialReference: 'https://git-scm.com/docs/git-sparse-checkout',
  },
];
