// ============================================================
// GitAtlas — Staging & Snapshots Commands
// git add, git status, git diff, git commit, git rm
// ============================================================

import type { GitCommand } from '../../types';

export const stagingCommands: GitCommand[] = [
  {
    id: 'add',
    name: 'add',
    executable: 'git',
    category: 'staging-snapshots',
    subcategory: 'Staging',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'safe',
    summary: 'Add file contents to the staging area (index)',
    description: 'git add takes modified or untracked files from your working directory and places them in the staging area (also called the index). The staging area is a buffer between your working directory and the repository — it lets you selectively choose which changes to include in your next commit.',
    whyItExists: 'Unlike some version control systems that commit everything at once, Git uses a two-step process: stage then commit. This gives you precise control over what goes into each commit, enabling clean, logical commit history.',
    whenToUse: [
      'Preparing specific files for the next commit',
      'Staging all changes in the working directory',
      'Interactively selecting which hunks (portions) of a file to stage',
      'Adding a new file to be tracked by Git',
    ],
    whenNotToUse: [
      'When you want to commit all tracked-file changes at once — use git commit -a instead',
      'When you want to undo staging — use git restore --staged',
    ],
    syntax: [
      'git add <pathspec>...',
      'git add .',
      'git add -A',
      'git add -p [<pathspec>...]',
      'git add -u',
    ],
    options: [
      { flag: '.', description: 'Stage all changes in the current directory and subdirectories (new, modified, deleted).', example: 'git add .', whenUseful: 'When you want to stage everything from the current directory down' },
      { flag: '-A', shortFlag: '--all', description: 'Stage all changes in the entire working tree (new, modified, deleted files).', example: 'git add -A', whenUseful: 'Staging everything regardless of your current directory' },
      { flag: '-p', shortFlag: '--patch', description: 'Interactively select hunks of changes to stage. You can stage part of a file.', example: 'git add -p src/app.ts', whenUseful: 'When a file has multiple logical changes and you want to commit them separately' },
      { flag: '-u', shortFlag: '--update', description: 'Stage modifications and deletions of tracked files only — does not add new untracked files.', example: 'git add -u', whenUseful: 'When you want to stage changes to existing files but not new ones' },
      { flag: '-n', shortFlag: '--dry-run', description: 'Show what would be staged without actually staging anything.', example: 'git add -n .', whenUseful: 'Previewing what will be staged before committing' },
      { flag: '-f', shortFlag: '--force', description: 'Allow adding files that are ignored by .gitignore.', example: 'git add -f build/output.js', whenUseful: 'When you explicitly need to track an ignored file', warning: 'Overrides .gitignore protection' },
    ],
    examples: [
      { title: 'Stage a single file', command: 'git add src/app.ts' },
      { title: 'Stage all changes', command: 'git add .' },
      { title: 'Stage interactively', command: 'git add -p', output: 'Shows each hunk of changes and lets you choose:\ny = stage, n = skip, s = split, q = quit' },
      { title: 'Stage multiple specific files', command: 'git add src/app.ts src/utils.ts README.md' },
    ],
    situations: [
      { description: 'You fixed a bug and refactored nearby code in the same file', command: 'git add -p src/handler.ts', explanation: 'Interactive mode lets you stage just the bug fix in one commit, then the refactor in another.' },
      { description: 'You want to stage everything for a quick commit', command: 'git add . && git commit -m "Update components"', explanation: 'Stages all changes from the current directory and creates a commit.' },
    ],
    mistakes: [
      'Running git add . from a subdirectory and thinking it staged everything — it only stages from the current directory down. Use git add -A for the entire repo.',
      'Staging files you didn\'t intend to — always run git status before committing.',
      'Accidentally staging sensitive files (credentials, .env) — set up .gitignore before running git add .',
    ],
    relatedCommands: ['status', 'commit', 'restore', 'reset', 'diff', 'rm'],
    comparisonCommands: ['commit'],
    tips: [
      'git add -p is one of the most powerful Git features for maintaining clean history.',
      'Use .gitignore to prevent sensitive or generated files from being staged accidentally.',
      'git add -N <file> marks a new file as "intent to add" without staging its content — useful with git diff.',
    ],
    officialReference: 'https://git-scm.com/docs/git-add',
  },

  {
    id: 'status',
    name: 'status',
    executable: 'git',
    category: 'staging-snapshots',
    subcategory: 'Inspection',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'safe',
    summary: 'Show the working tree status — what\'s staged, modified, or untracked',
    description: 'git status displays the state of your working directory and staging area. It shows which files are staged for the next commit, which have been modified but not staged, and which are untracked. It also tells you the current branch and its relationship to the upstream.',
    whyItExists: 'You need to know what state your repository is in before making decisions. git status is the primary way to understand what Git sees and what your next commit will contain.',
    whenToUse: [
      'Before every commit to verify what will be included',
      'After git add to confirm the correct files were staged',
      'When you\'re unsure what state the repository is in',
      'To check if your branch is ahead of or behind the remote',
    ],
    whenNotToUse: [
      'It\'s almost always appropriate to use git status — it has no side effects',
    ],
    syntax: [
      'git status',
      'git status -s',
      'git status -b',
      'git status --porcelain',
    ],
    options: [
      { flag: '-s', shortFlag: '--short', description: 'Show output in short format.', example: 'git status -s', whenUseful: 'Quick overview when you know what to look for' },
      { flag: '-b', shortFlag: '--branch', description: 'Show branch and tracking info even in short format.', example: 'git status -sb' },
      { flag: '--porcelain', description: 'Machine-readable output format, stable across Git versions.', example: 'git status --porcelain', whenUseful: 'Scripting and automation' },
      { flag: '--ignored', description: 'Also show ignored files.', example: 'git status --ignored' },
      { flag: '-u', shortFlag: '--untracked-files', description: 'Show untracked files. Use -uno to hide them.', example: 'git status -uno', whenUseful: 'Hiding noise from generated or build files' },
    ],
    examples: [
      { title: 'Check repository status', command: 'git status', output: 'On branch main\nYour branch is up to date with \'origin/main\'.\n\nChanges to be committed:\n  modified:   src/app.ts\n\nChanges not staged for commit:\n  modified:   src/utils.ts\n\nUntracked files:\n  src/newfile.ts' },
      { title: 'Short format', command: 'git status -s', output: 'M  src/app.ts\n M src/utils.ts\n?? src/newfile.ts' },
    ],
    situations: [
      { description: 'Checking what changed before committing', command: 'git status', explanation: 'Shows staged, unstaged, and untracked changes so you know exactly what your commit will contain.' },
    ],
    mistakes: [
      'Ignoring git status output and committing blindly — can lead to missing files or accidentally committing unwanted changes.',
    ],
    relatedCommands: ['add', 'diff', 'commit', 'restore'],
    tips: [
      'Make git status a habit — run it before every add and commit.',
      'The short format (M, A, D, ??) is faster to scan: first column = staged, second column = working tree.',
    ],
    officialReference: 'https://git-scm.com/docs/git-status',
  },

  {
    id: 'diff',
    name: 'diff',
    executable: 'git',
    category: 'staging-snapshots',
    subcategory: 'Inspection',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'safe',
    summary: 'Show changes between working directory, staging area, and commits',
    description: 'git diff shows the exact line-by-line differences between various states of your repository. Without arguments, it shows unstaged changes (working directory vs staging area). With --staged, it shows what\'s about to be committed. You can also compare branches, commits, or specific files.',
    whyItExists: 'Before committing, merging, or reviewing code, you need to see exactly what changed. git diff is the primary tool for inspecting modifications at the line level.',
    whenToUse: [
      'Reviewing changes before staging them',
      'Reviewing staged changes before committing',
      'Comparing two branches or commits',
      'Generating patches for code review',
    ],
    whenNotToUse: [
      'When you need a high-level overview — use git status instead',
      'When you need a graphical diff — use git difftool',
    ],
    syntax: [
      'git diff',
      'git diff --staged',
      'git diff <commit> <commit>',
      'git diff <branch>..<branch>',
      'git diff -- <path>',
    ],
    options: [
      { flag: '--staged', shortFlag: '--cached', description: 'Show the diff between the staging area and the last commit (what will be committed).', example: 'git diff --staged', whenUseful: 'Reviewing exactly what your next commit will contain' },
      { flag: '--stat', description: 'Show a summary of changes (files changed, insertions, deletions).', example: 'git diff --stat main..feature', whenUseful: 'Quick overview of change scope' },
      { flag: '--name-only', description: 'Show only the names of changed files.', example: 'git diff --name-only HEAD~3' },
      { flag: '--word-diff', description: 'Show differences at the word level instead of line level.', example: 'git diff --word-diff', whenUseful: 'Prose or documentation changes where line-level diffs are noisy' },
      { flag: '-U<n>', description: 'Show n lines of context around each change (default is 3).', example: 'git diff -U5' },
      { flag: '--color-words', description: 'Show changed words inline with color highlighting.', example: 'git diff --color-words' },
    ],
    examples: [
      { title: 'See unstaged changes', command: 'git diff' },
      { title: 'See staged changes', command: 'git diff --staged' },
      { title: 'Compare branches', command: 'git diff main..feature-auth' },
      { title: 'Compare specific file between commits', command: 'git diff HEAD~2 -- src/app.ts' },
      { title: 'Summary statistics', command: 'git diff --stat', output: ' src/app.ts  | 15 ++++++---\n src/utils.ts |  8 ++--\n 2 files changed, 14 insertions(+), 9 deletions(-)' },
    ],
    situations: [
      { description: 'Reviewing what you\'re about to commit', command: 'git add -A && git diff --staged', explanation: 'Stage everything, then review the complete diff before committing.' },
      { description: 'Checking what changed on a feature branch', command: 'git diff main..feature-branch --stat', explanation: 'Shows a summary of all files changed between main and the feature branch.' },
    ],
    mistakes: [
      'Running git diff and seeing nothing — this means either nothing has changed or all changes are already staged. Use git diff --staged.',
      'Confusing git diff (unstaged) with git diff --staged (staged). They show different things.',
    ],
    relatedCommands: ['status', 'log', 'show', 'difftool'],
    comparisonCommands: ['status', 'show'],
    tips: [
      'git diff shows unstaged changes; git diff --staged shows staged changes. Together they show the full picture.',
      'Pipe to less for large diffs: git diff | less',
    ],
    officialReference: 'https://git-scm.com/docs/git-diff',
  },

  {
    id: 'commit',
    name: 'commit',
    executable: 'git',
    category: 'staging-snapshots',
    subcategory: 'Snapshots',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'safe',
    summary: 'Record changes to the repository — create a new commit',
    description: 'git commit takes everything in the staging area and creates a permanent snapshot in the repository history. Each commit has a unique SHA-1 hash, records the author, timestamp, parent commit(s), and a message describing the change. Commits are the fundamental unit of Git\'s history.',
    whyItExists: 'Commits are Git\'s way of recording project state over time. They form a directed acyclic graph (DAG) that represents the entire history of the project, enabling branching, merging, rollback, and collaboration.',
    whenToUse: [
      'After staging changes with git add',
      'When a logical unit of work is complete',
      'After fixing a bug, implementing a feature, or making a meaningful change',
    ],
    whenNotToUse: [
      'When no changes are staged — git commit will fail or open an empty commit',
      'When changes are half-done and untested — prefer smaller, complete commits',
    ],
    syntax: [
      'git commit -m "<message>"',
      'git commit',
      'git commit -a -m "<message>"',
      'git commit --amend',
      'git commit --amend --no-edit',
      'git commit --allow-empty -m "<message>"',
    ],
    options: [
      { flag: '-m <msg>', shortFlag: '--message', description: 'Use the given message as the commit message.', example: 'git commit -m "Fix login validation"', whenUseful: 'Quick commits where the message fits in one line' },
      { flag: '-a', shortFlag: '--all', description: 'Automatically stage all modified and deleted tracked files before committing. Does not add new untracked files.', example: 'git commit -am "Update handlers"', whenUseful: 'Quick commits of all tracked-file changes' },
      { flag: '--amend', description: 'Replace the last commit with a new one that includes the current staging area.', example: 'git commit --amend -m "Better message"', whenUseful: 'Fixing a typo in the last commit message or adding a forgotten file', warning: 'Rewrites history — do not amend commits that have been pushed to a shared branch' },
      { flag: '--no-edit', description: 'With --amend, reuse the previous commit message without opening an editor.', example: 'git commit --amend --no-edit', whenUseful: 'Adding a forgotten file to the last commit without changing the message' },
      { flag: '--allow-empty', description: 'Allow creating a commit with no changes.', example: 'git commit --allow-empty -m "Trigger CI"', whenUseful: 'Triggering CI/CD pipelines or marking milestones' },
      { flag: '-v', shortFlag: '--verbose', description: 'Show the diff being committed in the editor.', example: 'git commit -v', whenUseful: 'Reviewing your changes while writing the commit message' },
      { flag: '--fixup=<commit>', description: 'Create a fixup commit for use with interactive rebase.', example: 'git commit --fixup=abc123', whenUseful: 'Organizing changes to be squashed during interactive rebase' },
    ],
    examples: [
      { title: 'Basic commit', command: 'git add . && git commit -m "Add user authentication"' },
      { title: 'Commit all tracked changes', command: 'git commit -am "Fix header alignment"' },
      { title: 'Amend the last commit', command: 'git add forgotten-file.ts\ngit commit --amend --no-edit' },
      { title: 'Verbose commit (see diff in editor)', command: 'git commit -v' },
    ],
    situations: [
      { description: 'You forgot to include a file in the last commit', command: 'git add missing-file.ts && git commit --amend --no-edit', explanation: 'Adds the missing file to the previous commit without creating a new one.' },
      { description: 'You want to rewrite the last commit message', command: 'git commit --amend -m "feat: add OAuth2 login flow"', explanation: 'Replaces the previous commit with a new one that has the updated message.' },
    ],
    mistakes: [
      'Writing vague commit messages like "fix" or "update" — be specific about what changed and why.',
      'Using --amend on commits that have already been pushed — this rewrites history and can cause problems for collaborators.',
      'Making huge commits with many unrelated changes — prefer small, focused commits.',
      'Committing without running git status first — you might include unintended files.',
    ],
    warnings: [
      'git commit --amend rewrites the last commit. If you\'ve already pushed, you\'ll need to force-push, which can disrupt collaborators.',
    ],
    relatedCommands: ['add', 'status', 'diff', 'log', 'reset', 'revert'],
    comparisonCommands: ['add'],
    tips: [
      'Follow conventional commit format: type(scope): description — e.g., feat(auth): add login form.',
      'Use git commit -v to see the diff while writing your message — helps write better messages.',
      'An empty git commit (without -m) opens your configured editor for a multi-line message.',
    ],
    officialReference: 'https://git-scm.com/docs/git-commit',
  },

  {
    id: 'rm',
    name: 'rm',
    executable: 'git',
    category: 'staging-snapshots',
    subcategory: 'File Management',
    type: 'porcelain',
    difficulty: 'beginner',
    dangerLevel: 'caution',
    summary: 'Remove files from the working tree and the staging area',
    description: 'git rm removes a file from both the working directory and the staging area, scheduling the deletion for the next commit. With --cached, it only removes the file from the staging area (stops tracking it) but keeps it in the working directory.',
    whyItExists: 'Simply deleting a file with rm doesn\'t tell Git to stop tracking it. git rm handles both the filesystem deletion and the Git tracking removal in one step.',
    whenToUse: [
      'Permanently removing a file from the project',
      'Untracking a file that was accidentally committed (with --cached)',
      'Removing files that are no longer needed',
    ],
    whenNotToUse: [
      'When you want to temporarily hide changes — use git stash instead',
      'When the file shouldn\'t have been tracked — add it to .gitignore and use git rm --cached',
    ],
    syntax: [
      'git rm <file>...',
      'git rm --cached <file>...',
      'git rm -r <directory>',
      'git rm -f <file>',
    ],
    options: [
      { flag: '--cached', description: 'Remove the file from the staging area only — keep it in the working directory. The file becomes untracked.', example: 'git rm --cached secrets.env', whenUseful: 'When you accidentally committed a file that should be in .gitignore' },
      { flag: '-r', description: 'Recursively remove a directory and all its contents.', example: 'git rm -r old-module/', whenUseful: 'Removing an entire directory from tracking' },
      { flag: '-f', shortFlag: '--force', description: 'Force removal even if the file has local modifications.', example: 'git rm -f modified-file.ts', warning: 'Discards unsaved changes in the file' },
      { flag: '-n', shortFlag: '--dry-run', description: 'Show what would be removed without actually doing it.', example: 'git rm -n *.log' },
    ],
    examples: [
      { title: 'Remove a file', command: 'git rm old-file.ts\ngit commit -m "Remove old-file.ts"' },
      { title: 'Stop tracking without deleting', command: 'git rm --cached .env\necho ".env" >> .gitignore\ngit commit -m "Stop tracking .env"' },
      { title: 'Remove a directory', command: 'git rm -r legacy/' },
    ],
    situations: [
      { description: 'You committed a .env file with secrets', command: 'git rm --cached .env && echo ".env" >> .gitignore && git commit -m "Remove .env from tracking"', explanation: 'Removes .env from Git tracking but keeps it locally. Future commits won\'t include it.' },
    ],
    mistakes: [
      'Using git rm when you only want to untrack a file — you\'ll delete it from disk. Use --cached.',
      'Forgetting to add the file to .gitignore after git rm --cached — it will be staged again on the next git add .',
      'Note: git rm --cached does not remove the file from history. Previous commits still contain it. For sensitive data, you need git filter-branch or BFG Repo-Cleaner.',
    ],
    relatedCommands: ['add', 'restore', 'reset', 'clean'],
    tips: [
      'After git rm --cached, always update .gitignore to prevent the file from being re-added.',
      'To see what git rm would do without executing: git rm -n <file>',
    ],
    officialReference: 'https://git-scm.com/docs/git-rm',
  },
];
