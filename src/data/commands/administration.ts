// ============================================================
// GitAtlas — Administration Commands
// git gc, git fsck, git prune, git maintenance, git count-objects
// ============================================================
import type { GitCommand } from '../../types';

export const administrationCommands: GitCommand[] = [
  {
    id: 'gc', name: 'gc', executable: 'git', category: 'administration', subcategory: 'Maintenance', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'safe',
    summary: 'Cleanup and optimize the repository — garbage collection',
    description: 'git gc (garbage collection) cleans up unnecessary files and optimizes the local repository. It compresses file revisions, removes unreachable objects, packs refs, and runs other housekeeping tasks. Git runs gc automatically in the background, but you can trigger it manually.',
    whyItExists: 'Over time, Git accumulates loose objects, orphaned refs, and unoptimized data. gc compresses and cleans up this data to save disk space and improve performance.',
    whenToUse: ['After many operations that create loose objects','When the repository feels slow','After deleting many branches','As periodic maintenance'],
    whenNotToUse: ['In the middle of other operations — gc can conflict with concurrent processes'],
    syntax: ['git gc','git gc --aggressive','git gc --auto','git gc --prune=<date>'],
    options: [
      { flag: '--aggressive', description: 'More thorough optimization (slower, better compression).', example: 'git gc --aggressive' },
      { flag: '--auto', description: 'Only run if Git determines it\'s needed.', example: 'git gc --auto' },
      { flag: '--prune=<date>', description: 'Remove loose objects older than the given date.', example: 'git gc --prune=now', warning: 'Using --prune=now removes objects immediately — may lose unreachable objects' },
    ],
    examples: [
      { title: 'Run garbage collection', command: 'git gc' },
      { title: 'Aggressive optimization', command: 'git gc --aggressive --prune=now' },
      { title: 'Check if gc is needed', command: 'git gc --auto' },
    ],
    situations: [{ description: 'Repository feels slow after many rebases', command: 'git gc --aggressive', explanation: 'Performs thorough cleanup and recompression of objects.' }],
    mistakes: ['Running gc --aggressive on every commit — it\'s slow and usually unnecessary.','Using --prune=now before checking reflog for recoverable commits.'],
    relatedCommands: ['prune', 'fsck', 'count-objects', 'maintenance'],
    tips: ['Git runs gc automatically — manual runs are rarely needed.','gc --aggressive is mainly useful after importing large repositories or many history rewrites.'],
    officialReference: 'https://git-scm.com/docs/git-gc',
  },
  {
    id: 'fsck', name: 'fsck', executable: 'git', category: 'administration', subcategory: 'Integrity', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'safe',
    summary: 'Verify the integrity of objects in the Git database',
    description: 'git fsck (file system check) validates the connectivity and integrity of Git objects. It checks that all objects are reachable, properly formatted, and not corrupted. It can also find dangling (unreachable) objects.',
    whyItExists: 'Repository corruption can happen from disk errors, interrupted operations, or bugs. fsck provides a way to verify that the object database is consistent and find any problems.',
    whenToUse: ['Diagnosing repository corruption','Finding dangling/unreachable objects','Recovering lost objects that reflog can\'t find','Verifying repository health'],
    whenNotToUse: ['For routine maintenance — use git gc instead'],
    syntax: ['git fsck','git fsck --full','git fsck --unreachable','git fsck --dangling','git fsck --lost-found'],
    options: [
      { flag: '--full', description: 'Check full object connectivity (slower but more thorough).', example: 'git fsck --full' },
      { flag: '--unreachable', description: 'Show unreachable objects.', example: 'git fsck --unreachable' },
      { flag: '--dangling', description: 'Show dangling objects (default).', example: 'git fsck --dangling' },
      { flag: '--lost-found', description: 'Write dangling objects into .git/lost-found/.', example: 'git fsck --lost-found' },
    ],
    examples: [
      { title: 'Check repository integrity', command: 'git fsck', output: 'Checking object directories: 100%\ndangling commit abc1234...\ndangling blob def5678...' },
      { title: 'Find lost objects', command: 'git fsck --lost-found' },
    ],
    situations: [{ description: 'Recovering a commit that reflog can\'t find', command: 'git fsck --unreachable | grep commit', explanation: 'Lists unreachable commits — you might find the one you lost.' }],
    mistakes: ['Confusing dangling objects with corruption — dangling objects are normal after rebase/reset.'],
    relatedCommands: ['gc', 'reflog', 'prune'],
    tips: ['Dangling objects are normal — they\'re cleaned up by gc.','Use git show <hash> to inspect dangling commits found by fsck.'],
    officialReference: 'https://git-scm.com/docs/git-fsck',
  },
  {
    id: 'prune', name: 'prune', executable: 'git', category: 'administration', subcategory: 'Maintenance', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'caution',
    summary: 'Remove unreachable objects from the object database',
    description: 'git prune removes objects that are no longer reachable from any reference (branch, tag, or reflog entry). It\'s typically run as part of git gc and rarely needs to be invoked directly.',
    whyItExists: 'After operations like reset, rebase, or branch deletion, some objects become unreachable. Prune removes them to reclaim disk space.',
    whenToUse: ['Reclaiming disk space after many history-rewriting operations','As part of manual repository cleanup'],
    whenNotToUse: ['When you might need to recover objects via reflog — prune deletes them permanently','When git gc handles it automatically'],
    syntax: ['git prune','git prune --dry-run','git prune --expire <time>'],
    options: [
      { flag: '-n', shortFlag: '--dry-run', description: 'Show what would be removed.', example: 'git prune -n' },
      { flag: '--expire <time>', description: 'Only prune objects older than the given time.', example: 'git prune --expire 2.weeks.ago' },
    ],
    examples: [
      { title: 'Preview what would be pruned', command: 'git prune -n' },
      { title: 'Prune old objects', command: 'git prune --expire 2.weeks.ago' },
    ],
    situations: [],
    mistakes: ['Running prune before checking reflog — you may lose recoverable objects.'],
    warnings: ['git prune permanently removes objects. Unlike gc, it doesn\'t check reflogs by default.'],
    relatedCommands: ['gc', 'fsck', 'reflog'],
    tips: ['Usually you don\'t need to run prune directly — git gc handles it.','Prune respects reflog expiry by default when called via gc.'],
    officialReference: 'https://git-scm.com/docs/git-prune',
  },
  {
    id: 'maintenance', name: 'maintenance', executable: 'git', category: 'administration', subcategory: 'Maintenance', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'safe',
    summary: 'Schedule and run repository maintenance tasks automatically',
    description: 'git maintenance provides a framework for running repository maintenance tasks on a schedule. It can run tasks like gc, commit-graph updates, and prefetching in the background without blocking your workflow.',
    whyItExists: 'Running gc manually is tedious. git maintenance automates repository upkeep with configurable scheduling, running tasks like garbage collection and commit-graph updates in the background.',
    whenToUse: ['Automating repository maintenance','Large repositories that benefit from regular optimization','Setting up background prefetching'],
    whenNotToUse: ['For small repositories where automatic gc is sufficient'],
    syntax: ['git maintenance start','git maintenance stop','git maintenance run','git maintenance register','git maintenance unregister'],
    options: [
      { flag: 'start', description: 'Start the maintenance schedule (cron/launchd/scheduled task).', example: 'git maintenance start' },
      { flag: 'stop', description: 'Stop scheduled maintenance.', example: 'git maintenance stop' },
      { flag: 'run', description: 'Run maintenance tasks now.', example: 'git maintenance run' },
      { flag: 'register', description: 'Register the current repo for scheduled maintenance.', example: 'git maintenance register' },
      { flag: '--task=<task>', description: 'Run a specific task.', example: 'git maintenance run --task=gc' },
    ],
    examples: [
      { title: 'Start automatic maintenance', command: 'git maintenance start' },
      { title: 'Run maintenance now', command: 'git maintenance run' },
      { title: 'Stop automatic maintenance', command: 'git maintenance stop' },
    ],
    situations: [{ description: 'Optimizing a large monorepo', command: 'git maintenance start', explanation: 'Sets up background tasks to keep the repo fast.' }],
    mistakes: [],
    relatedCommands: ['gc', 'prune', 'fsck'],
    tips: ['Requires Git 2.29+.','Tasks include: gc, commit-graph, prefetch, loose-objects, incremental-repack.'],
    officialReference: 'https://git-scm.com/docs/git-maintenance',
  },
  {
    id: 'count-objects', name: 'count-objects', executable: 'git', category: 'administration', subcategory: 'Information', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'safe',
    summary: 'Count the number of objects in the repository and their disk consumption',
    description: 'git count-objects reports the number of loose objects, their total size, the number of packed objects, and pack file sizes. Useful for understanding repository size and determining if gc is needed.',
    whyItExists: 'Understanding how much storage a repository uses and whether it has many loose objects helps decide when to run gc or investigate bloat.',
    whenToUse: ['Checking repository size','Determining if gc is needed','Diagnosing why a repo is large'],
    whenNotToUse: [],
    syntax: ['git count-objects','git count-objects -v','git count-objects -H'],
    options: [
      { flag: '-v', shortFlag: '--verbose', description: 'Show detailed counts.', example: 'git count-objects -v' },
      { flag: '-H', shortFlag: '--human-readable', description: 'Show sizes in human-readable format.', example: 'git count-objects -vH' },
    ],
    examples: [
      { title: 'Count objects', command: 'git count-objects -vH', output: 'count: 42\nsize: 168.00 KiB\nin-pack: 1543\npacks: 1\nsize-pack: 2.10 MiB\n...' },
    ],
    situations: [{ description: 'Checking if gc is needed', command: 'git count-objects -v', explanation: 'A high loose object count suggests gc would help.' }],
    mistakes: [],
    relatedCommands: ['gc', 'fsck', 'prune'],
    tips: ['Many loose objects = gc will help.','size-pack shows the actual disk usage for packed objects.'],
    officialReference: 'https://git-scm.com/docs/git-count-objects',
  },
];
