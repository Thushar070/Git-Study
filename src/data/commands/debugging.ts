// ============================================================
// GitAtlas — Debugging Commands
// git grep, git diff (adv), git apply, git format-patch, git am
// ============================================================
import type { GitCommand } from '../../types';

export const debuggingCommands: GitCommand[] = [
  {
    id: 'grep', name: 'grep', executable: 'git', category: 'debugging', subcategory: 'Search', type: 'porcelain', difficulty: 'intermediate', dangerLevel: 'safe',
    summary: 'Search tracked files for a pattern',
    description: 'git grep searches through tracked files in the working tree (or at a specific commit) for lines matching a pattern. It\'s faster than regular grep because it only searches Git-tracked files and leverages Git\'s internal index.',
    whyItExists: 'Regular grep searches all files including untracked ones, node_modules, build artifacts, etc. git grep is faster and more focused — it only searches files Git knows about.',
    whenToUse: ['Searching for function names, variables, or patterns across the codebase','Finding all references to a specific string','Searching at a specific commit without checkout'],
    whenNotToUse: ['When you need to search untracked or ignored files — use regular grep'],
    syntax: ['git grep <pattern>','git grep -n <pattern>','git grep <pattern> <commit>','git grep -l <pattern>'],
    options: [
      { flag: '-n', shortFlag: '--line-number', description: 'Show line numbers.', example: 'git grep -n "TODO"' },
      { flag: '-l', shortFlag: '--files-with-matches', description: 'Show only filenames.', example: 'git grep -l "deprecated"' },
      { flag: '-c', shortFlag: '--count', description: 'Show count of matches per file.', example: 'git grep -c "console.log"' },
      { flag: '-i', shortFlag: '--ignore-case', description: 'Case-insensitive search.', example: 'git grep -i "error"' },
      { flag: '-w', shortFlag: '--word-regexp', description: 'Match whole words only.', example: 'git grep -w "user"' },
      { flag: '-e <pattern>', description: 'Specify pattern (allows multiple with --or/--and).', example: 'git grep -e "TODO" --or -e "FIXME"' },
    ],
    examples: [
      { title: 'Search for a pattern', command: 'git grep -n "handleLogin"', output: 'src/auth/login.ts:42: function handleLogin(data) {\nsrc/auth/login.ts:89:   return handleLogin(formData);' },
      { title: 'Find TODOs', command: 'git grep -n "TODO\\|FIXME\\|HACK"' },
      { title: 'Search in a specific commit', command: 'git grep "oldFunction" HEAD~10' },
      { title: 'Count matches per file', command: 'git grep -c "console.log"' },
    ],
    situations: [
      { description: 'Finding all uses of a deprecated function', command: 'git grep -n "oldAPICall"', explanation: 'Shows every file and line where the deprecated function is referenced.' },
    ],
    mistakes: ['Forgetting that git grep only searches tracked files.'],
    relatedCommands: ['log', 'blame', 'diff'],
    tips: ['Faster than grep for tracked files because it uses Git\'s index.','Use git grep <pattern> <commit> to search old versions without checkout.'],
    officialReference: 'https://git-scm.com/docs/git-grep',
  },
  {
    id: 'apply', name: 'apply', executable: 'git', category: 'debugging', subcategory: 'Patching', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'caution',
    summary: 'Apply a patch file to the working directory',
    description: 'git apply reads a unified diff (patch) file and applies the changes to your working directory. Unlike git am, it does not create a commit — it just modifies files.',
    whyItExists: 'When someone sends you a patch file (from git diff output or git format-patch), git apply lets you apply those changes without the commit metadata.',
    whenToUse: ['Applying patches from email or external sources','Testing changes before committing','Applying partial patches'],
    whenNotToUse: ['When the patch includes commit metadata — use git am instead'],
    syntax: ['git apply <patch-file>','git apply --check <patch-file>','git apply --stat <patch-file>','git apply -R <patch-file>'],
    options: [
      { flag: '--check', description: 'Test if the patch applies cleanly without modifying files.', example: 'git apply --check fix.patch' },
      { flag: '--stat', description: 'Show statistics about the patch.', example: 'git apply --stat fix.patch' },
      { flag: '-R', shortFlag: '--reverse', description: 'Apply the patch in reverse.', example: 'git apply -R fix.patch' },
      { flag: '--3way', description: 'Attempt a 3-way merge if the patch doesn\'t apply directly.', example: 'git apply --3way fix.patch' },
    ],
    examples: [
      { title: 'Check if patch applies', command: 'git apply --check bugfix.patch' },
      { title: 'Apply a patch', command: 'git apply bugfix.patch' },
      { title: 'Reverse a patch', command: 'git apply -R bugfix.patch' },
    ],
    situations: [{ description: 'Someone emailed you a patch', command: 'git apply --check fix.patch && git apply fix.patch', explanation: 'Verifies the patch applies cleanly, then applies it.' }],
    mistakes: ['Applying patches to the wrong branch.','Not checking first with --check.'],
    relatedCommands: ['diff', 'format-patch', 'am'],
    tips: ['Always --check before applying.','git apply does not create commits — stage and commit manually.'],
    officialReference: 'https://git-scm.com/docs/git-apply',
  },
  {
    id: 'format-patch', name: 'format-patch', executable: 'git', category: 'debugging', subcategory: 'Patching', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'safe',
    summary: 'Prepare patches for email submission — export commits as patch files',
    description: 'git format-patch generates patch files from commits, formatted as email messages with commit metadata (author, date, message, diff). These can be sent via email and applied with git am.',
    whyItExists: 'The Linux kernel development workflow relies on emailing patches. format-patch creates properly formatted patch files from commits for this workflow.',
    whenToUse: ['Sharing commits via email or file transfer','Creating portable patches for code review','Exporting specific commits for application elsewhere'],
    whenNotToUse: ['When you can use pull requests or merge requests — those are more modern workflows'],
    syntax: ['git format-patch -<n>','git format-patch <since>','git format-patch <from>..<to>','git format-patch -o <dir> HEAD~3'],
    options: [
      { flag: '-<n>', description: 'Create patches for the last n commits.', example: 'git format-patch -3' },
      { flag: '-o <dir>', shortFlag: '--output-directory', description: 'Write patches to a directory.', example: 'git format-patch -o patches/ HEAD~5' },
      { flag: '--stdout', description: 'Print patches to stdout.', example: 'git format-patch --stdout HEAD~1' },
    ],
    examples: [
      { title: 'Create patches for last 3 commits', command: 'git format-patch -3', output: '0001-Add-login-form.patch\n0002-Add-validation.patch\n0003-Fix-edge-case.patch' },
      { title: 'Create patches since a tag', command: 'git format-patch v1.0' },
    ],
    situations: [{ description: 'Exporting commits for a team member who can\'t access the remote', command: 'git format-patch -5 -o /tmp/patches/', explanation: 'Creates 5 patch files that can be transferred and applied with git am.' }],
    mistakes: [],
    relatedCommands: ['am', 'apply', 'send-email'],
    tips: ['Recipients apply patches with git am.','Patch files include commit metadata — apply preserves authorship.'],
    officialReference: 'https://git-scm.com/docs/git-format-patch',
  },
  {
    id: 'am', name: 'am', executable: 'git', category: 'debugging', subcategory: 'Patching', type: 'porcelain', difficulty: 'advanced', dangerLevel: 'caution',
    summary: 'Apply patches from mailbox messages — apply format-patch output',
    description: 'git am (apply mailbox) applies patch files created by git format-patch. Unlike git apply, it creates commits with the original author, date, and message preserved.',
    whyItExists: 'The complementary command to git format-patch. While apply just modifies files, am creates proper commits from patch files, preserving full commit metadata.',
    whenToUse: ['Applying patches received via email','Importing commits from format-patch output'],
    whenNotToUse: ['When the patch has no commit metadata — use git apply instead'],
    syntax: ['git am <patch-file>...','git am <mbox-file>','git am --abort','git am --continue'],
    options: [
      { flag: '--abort', description: 'Abort the current am operation.', example: 'git am --abort' },
      { flag: '--continue', description: 'Continue after resolving conflicts.', example: 'git am --continue' },
      { flag: '--skip', description: 'Skip the current patch.', example: 'git am --skip' },
      { flag: '-3', shortFlag: '--3way', description: 'Attempt 3-way merge on conflicts.', example: 'git am -3 fix.patch' },
    ],
    examples: [
      { title: 'Apply patches', command: 'git am patches/*.patch' },
      { title: 'Apply with 3-way merge', command: 'git am -3 0001-Fix-login.patch' },
    ],
    situations: [{ description: 'Applying patches from a teammate', command: 'git am -3 patches/*.patch', explanation: 'Applies all patch files as commits with original metadata.' }],
    mistakes: ['Applying patches out of order.','Not using -3 when patches don\'t apply cleanly.'],
    relatedCommands: ['format-patch', 'apply'],
    tips: ['Use -3 for better conflict resolution.','git am creates commits — unlike git apply which only modifies files.'],
    officialReference: 'https://git-scm.com/docs/git-am',
  },
  {
    id: 'difftool', name: 'difftool', executable: 'git', category: 'debugging', subcategory: 'Inspection', type: 'porcelain', difficulty: 'intermediate', dangerLevel: 'safe',
    summary: 'Show changes using a visual diff tool',
    description: 'git difftool launches an external diff tool to view changes. It accepts the same arguments as git diff but opens a graphical comparison tool instead of showing text diffs in the terminal.',
    whyItExists: 'Terminal diffs can be hard to read for complex changes. Graphical diff tools like VS Code, meld, or Beyond Compare provide side-by-side visual comparison.',
    whenToUse: ['Reviewing complex diffs visually','When terminal diffs are hard to follow','Comparing binary files'],
    whenNotToUse: ['For simple, quick diffs — git diff in the terminal is faster'],
    syntax: ['git difftool','git difftool --staged','git difftool <commit> <commit>','git difftool --tool=<tool>'],
    options: [
      { flag: '--tool=<tool>', shortFlag: '-t <tool>', description: 'Use a specific diff tool.', example: 'git difftool --tool=meld' },
      { flag: '--staged', shortFlag: '--cached', description: 'Compare staged changes.', example: 'git difftool --staged' },
      { flag: '--no-prompt', shortFlag: '-y', description: 'Don\'t prompt before launching.', example: 'git difftool -y' },
      { flag: '--dir-diff', shortFlag: '-d', description: 'Compare directories instead of file-by-file.', example: 'git difftool -d HEAD~1' },
    ],
    examples: [
      { title: 'Open visual diff', command: 'git difftool' },
      { title: 'Compare with a specific tool', command: 'git difftool --tool=vscode' },
      { title: 'Configure VS Code as diff tool', command: 'git config --global diff.tool vscode\ngit config --global difftool.vscode.cmd \'code --wait --diff $LOCAL $REMOTE\'' },
    ],
    situations: [],
    mistakes: ['Not having a diff tool configured — set one up with git config.'],
    relatedCommands: ['diff', 'mergetool'],
    tips: ['Most IDEs have built-in diff viewers that work better for daily use.','Configure once: git config --global diff.tool <name>'],
    officialReference: 'https://git-scm.com/docs/git-difftool',
  },
];
